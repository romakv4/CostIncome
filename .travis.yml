matrix:
  include:
    - language: csharp
      mono: none
      dotnet: 3.1.100
      solution: CostIncome.sln
      matrix:
        include:
          - name: Build on Linux
            os: linux
            script:
              - dotnet build ./CostIncomeCalculator/CostIncomeCalculator.csproj
          - name: Test on Linux
            os: linux
            script:
              - dotnet test
          - name: Build on OSX
            os: osx
            osx_image: xcode11.3
            script:
              - dotnet build ./CostIncomeCalculator/CostIncomeCalculator.csproj
          - name: Test on OSX
            os: osx
            osx_image: xcode11.3
            script:
              - dotnet test
    - language: node.js
      node_js: "12.0"
      before_script:
        - cd client
      script:
        - npm install
        - npm run build