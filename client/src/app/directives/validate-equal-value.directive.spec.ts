import { ValidateEqualValueDirective } from './validate-equal-value.directive';

describe('ValidateEqualValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateEqualValueDirective();
    expect(directive).toBeTruthy();
  });
});
